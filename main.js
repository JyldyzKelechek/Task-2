    // Пункт 1
    let message = "ddd@bbb@ccc";
    let newMessage = text.replaceAll("@", "!");
    console.log(newMessage);

    // Пункт 2
    let message1 = 'js';
    console.log(message1.toUpperCase());

    // Пункт 3
    let message2 ='JS';
    console.log(message2.toLowerCase());

    // Пункт 4
    let message4 = 'I am a hero!';
    console.log(message4.length);

    // Пункт 5
     let creditCard = prompt("Введите номер кредитной карты:");
    let lastCreditCardNum = ("**********+numCard.slice(12)");
    document.write(`Номер вашей карты: ${lastCreditCardNum}`);


